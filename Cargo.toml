[workspace]
members = [
    "testing",
    "setup",
    "optimization",
    "simulation",
    "simulation-derive",
    "qrsim",
]

[profile.dev]
panic = "abort"

[profile.release]
lto = "thin"
panic = "abort"
